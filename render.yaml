services:
  - type: web
    name: urban-pulse-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python -m api.app
    envVars:
      - key: FLASK_ENV
        value: production
      - key: PORT
        value: 5000
    autoDeploy: true
